apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  namespace: mysql-replicaset

spec:
  selector:
    app: mysql-replica
  publishNotReadyAddresses: true
  clusterIP: None
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
